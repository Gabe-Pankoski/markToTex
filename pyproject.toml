[project]
name = "mark-to-tex"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pypandoc",
]

[project.scripts]
mark-to-tex = "main:main"
